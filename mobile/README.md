# Havilah Flutter Shell

Run the app with the standard Flutter workflow:

```bash
flutter pub get
flutter run
```

The scaffold ships with a three-tab navigation layout so new feature teams can plug in real content for the dashboard, note uploads, and learner profile screens.
